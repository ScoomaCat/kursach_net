{% extends 'base.html.twig' %}

{% block title %}{{ page.title }}{% endblock %}

{% block body %}
    <div class="container pt-5">
        <h1>{{ page.title }}</h1>

        <div>
            {{ page.content|raw }}
        </div>
    </div>
{% endblock %}
